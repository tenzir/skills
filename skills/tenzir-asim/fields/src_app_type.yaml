name: SrcAppType
summary: The type of the source application. For a list of allowed values and further information, refer to AppType in the Schema Overview article. This field is mandatory if SrcAppName or SrcAppId are used.
schemas:
- name: NetworkSession
  role: source
  class: Optional
  type: AppType
  required_if:
  - SrcAppName or SrcAppId are used
  description: The type of the source application. For a list of allowed values and further information, refer to AppType in the Schema Overview article. This field is mandatory if SrcAppName or SrcAppId are used.

# Security Lens

Use this lens when the diff touches user input, authentication, networking,
filesystems, secrets, or external command execution.

Focus on:

- input validation and sanitization
- injection risks
- credentials or secrets exposure
- authentication and authorization checks
- unsafe defaults or bypasses in sensitive code paths

Security findings should explain both the exploit path and the likely impact.

name: worker
used_by:
- model: Splunk_Audit
  dataset: Modular_Actions
  source: calculated
  type: string
  description: The worker performing the action.
  calculation:
    id: Modular_Actions_worker
    type: Eval
    expression: if(isnotnull(worker) AND worker!="","unknown",worker)

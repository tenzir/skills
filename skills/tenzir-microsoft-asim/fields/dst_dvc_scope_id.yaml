name: DstDvcScopeId
summary: The cloud platform scope ID the device belongs to. DstDvcScopeId map to a subscription ID on Azure and to an account ID on AWS.
schemas:
- name: Dns
  role: destination
  class: Optional
  type: String
  description: The cloud platform scope ID the device belongs to. DstDvcScopeId map to a subscription ID on Azure and to an account ID on AWS.
- name: NetworkSession
  role: destination
  class: Optional
  type: String
  description: The cloud platform scope ID the device belongs to. DstDvcScopeId map to a subscription ID on Azure and to an account ID on AWS.

name: TcpFlagsRst
summary: The TCP RST Flag reported. The reset flag gets sent from the receiver to the sender when a packet is sent to a particular host that was not expecting it.
schemas:
- name: NetworkSession
  role: network
  class: Optional
  type: Boolean
  description: The TCP RST Flag reported. The reset flag gets sent from the receiver to the sender when a packet is sent to a particular host that was not expecting it.

name: TargetFileName
summary: The name of the target file, without a path or a location, but with an extension if relevant. This field should be similar to the final element in the TargetFilePath field.
schemas:
- name: FileEvent
  role: target
  class: Recommended
  type: String
  description: The name of the target file, without a path or a location, but with an extension if relevant. This field should be similar to the final element in the TargetFilePath field.
